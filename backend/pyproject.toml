[project]
name = "optimizer"
version = "0.1.0"
description = "Log optimization + embedding service (RAG ingestion)"
requires-python = ">=3.12"
dependencies = [
  "fastapi[standard]>=0.128.1",
  "asyncpg>=0.30.0",
  "httpx>=0.27.0",
]
